{
  "name": "kiro",
  "version": "1.16.0",
  "description": "Claude plans, Kiro implements and reviews — a cost-savings delegation plugin. Claude designs specs and verifies results while Kiro CLI (subscription credits) writes the code inside an isolated worktree and can optionally review diffs before commit.",
  "author": {
    "name": "atomoh"
  },
  "agents": [
    "./agents/kiro-delegate-agent.md"
  ],
  "skills": [
    "./skills/kiro-delegate"
  ],
  "commands": [
    "./commands/delegate.md",
    "./commands/review.md",
    "./commands/setup.md",
    "./commands/configure.md"
  ],
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "bash \"${CLAUDE_PLUGIN_ROOT}/hooks/pre-commit-review.sh\""
          },
          {
            "type": "command",
            "command": "bash \"${CLAUDE_PLUGIN_ROOT}/hooks/pre-push-review.sh\""
          }
        ]
      }
    ],
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "bash \"${CLAUDE_PLUGIN_ROOT}/hooks/session-routing.sh\""
          },
          {
            "type": "command",
            "command": "bash -c 'for pid in $(pgrep -f \"${KIRO_REAP_PATTERN:-kiro-cli/kas/.*/@kiro/agent/dist/server/acp-server\\.js}\" 2>/dev/null); do [ \"$(ps -o ppid= -p \"$pid\" 2>/dev/null | tr -d \" \")\" = \"1\" ] && kill \"$pid\" 2>/dev/null; done; exit 0'"
          }
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "bash -c 'for pid in $(pgrep -f \"${KIRO_REAP_PATTERN:-kiro-cli/kas/.*/@kiro/agent/dist/server/acp-server\\.js}\" 2>/dev/null); do [ \"$(ps -o ppid= -p \"$pid\" 2>/dev/null | tr -d \" \")\" = \"1\" ] && kill \"$pid\" 2>/dev/null; done; exit 0'"
          }
        ]
      }
    ]
  }
}
