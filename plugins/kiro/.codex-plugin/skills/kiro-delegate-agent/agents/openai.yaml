interface:
  display_name: "Kiro Delegate Agent"
  short_description: "Cost-savings delegation orchestrator — host plans, Kiro CLI implements on flat-rate credits in an isolated worktree, host verifies with tests. Triggers on 'kiro로 구현', 'delegate implementation to..."
policy:
  allow_implicit_invocation: false
