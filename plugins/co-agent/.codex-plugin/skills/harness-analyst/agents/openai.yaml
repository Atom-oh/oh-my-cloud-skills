interface:
  display_name: "Harness Analyst"
  short_description: "Hill-climbing analyst for co-agent harness/consensus runs — invoke when accumulated .claude/co-agent-consensus/ run records should be mined into proposed /co-agent:configure adjustments..."
policy:
  allow_implicit_invocation: false
