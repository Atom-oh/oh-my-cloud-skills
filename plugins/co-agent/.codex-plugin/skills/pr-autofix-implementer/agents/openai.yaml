interface:
  display_name: "Pr Autofix Implementer"
  short_description: "Internal pipeline stage (edit-only application) of a co-agent plugin skill. Without the prepared working directory that skill supplies it does nothing except return blocked. Never choose this..."
policy:
  allow_implicit_invocation: false
